// const formatString = function(string) {
//     let newMassage= string.length> 40 ? `${string.slice(0,40)}...` :string;
//     return newMassage;
//   };
//   console.log(formatString("Если длина больше 40 символов, то функция обрезает строку до 40-ка символов и добавляет в конец строки троеточие"));